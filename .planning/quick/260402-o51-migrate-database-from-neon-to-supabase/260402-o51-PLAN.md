---
phase: quick
plan: 260402-o51
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/index.ts
  - package.json
  - .env.local
autonomous: true
must_haves:
  truths:
    - "App connects to Supabase PostgreSQL instead of Neon"
    - "Existing tests pass without modification (db is mocked)"
    - "Schema is applied to Supabase via drizzle-kit push"
    - "npm run build succeeds"
  artifacts:
    - path: "src/db/index.ts"
      provides: "Drizzle client using postgres-js driver"
      contains: "drizzle-orm/postgres-js"
    - path: "package.json"
      provides: "postgres dependency, no @neondatabase/serverless"
  key_links:
    - from: "src/db/index.ts"
      to: "src/app/actions/actions.ts"
      via: "import { db } from '@/db'"
      pattern: "import.*db.*from.*@/db"
---

<objective>
Migrate the database connection from Neon (@neondatabase/serverless + drizzle-orm/neon-http) to Supabase (postgres-js + drizzle-orm/postgres-js). The schema stays identical — only the driver and connection string change.

Purpose: Switch to Supabase as the database provider.
Output: Working Supabase connection, schema pushed, tests green, build clean.
</objective>

<execution_context>
@/Users/nicolaskw/Documents/ZalesMachine/Projects/claude-code-tutorial-lead-magnet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolaskw/Documents/ZalesMachine/Projects/claude-code-tutorial-lead-magnet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/db/index.ts
@src/db/schema.ts
@src/app/actions/actions.ts
@drizzle.config.ts
@package.json

<interfaces>
<!-- The db export is the only contract. All consumers import { db } from '@/db'. -->
<!-- The schema (src/db/schema.ts) is unchanged — both Neon and Supabase are PostgreSQL. -->
<!-- drizzle.config.ts reads DATABASE_URL and is dialect-agnostic — no changes needed. -->

From src/db/index.ts (CURRENT — will be replaced):
```typescript
import { neon } from '@neondatabase/serverless';
import { drizzle } from 'drizzle-orm/neon-http';
const sql = neon(process.env.DATABASE_URL!);
export const db = drizzle({ client: sql });
```

From src/db/schema.ts (UNCHANGED):
```typescript
export const users = pgTable('users', { ... });
```

From src/app/actions/actions.ts (consumer — NOT modified):
```typescript
import { db } from '@/db';
import { users } from '@/db/schema';
// uses db.insert(users).values(...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap driver dependencies</name>
  <files>package.json, src/db/index.ts</files>
  <action>
1. Run `npm uninstall @neondatabase/serverless` to remove the Neon driver.
2. Run `npm install postgres` to add the postgres-js driver.
3. Rewrite `src/db/index.ts` to:
   ```typescript
   import { drizzle } from 'drizzle-orm/postgres-js';
   import postgres from 'postgres';

   const client = postgres(process.env.DATABASE_URL!);
   export const db = drizzle({ client });
   ```
   This is a drop-in replacement — the `db` export keeps the same Drizzle API surface. No other files need changes because `drizzle-orm/postgres-js` exposes the same query builder as `drizzle-orm/neon-http`.
  </action>
  <verify>
    <automated>cd /Users/nicolaskw/Documents/ZalesMachine/Projects/claude-code-tutorial-lead-magnet && npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <done>All 5 existing tests pass. package.json has `postgres` dependency and no `@neondatabase/serverless`. src/db/index.ts uses drizzle-orm/postgres-js.</done>
</task>

<task type="auto">
  <name>Task 2: Set DATABASE_URL and push schema to Supabase</name>
  <files>.env.local</files>
  <action>
1. Retrieve the Supabase database connection string. Use the MCP tool `mcp__claude_ai_Supabase__get_project` with project ref `qxhlwgpobiveezhvsxqx` to find the database host/connection details OR construct the connection pooler URL:
   `postgresql://postgres.qxhlwgpobiveezhvsxqx:[PASSWORD]@aws-0-us-east-1.pooler.supabase.com:6543/postgres`
   The password can be retrieved from Supabase project settings.

2. Update `.env.local` to set `DATABASE_URL` to the Supabase connection string. Keep any other existing env vars.

3. Run `npx drizzle-kit push` to apply the existing schema (users table) to Supabase. This uses drizzle.config.ts which reads DATABASE_URL — no changes needed to that file.

4. Verify the push succeeded by checking drizzle-kit output shows the users table created/confirmed.

5. Run `npm run build` to confirm the Next.js build succeeds with the new driver.
  </action>
  <verify>
    <automated>cd /Users/nicolaskw/Documents/ZalesMachine/Projects/claude-code-tutorial-lead-magnet && npx drizzle-kit push 2>&1 | tail -10 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>DATABASE_URL points to Supabase. `drizzle-kit push` applied schema successfully. `npm run build` succeeds.</done>
</task>

</tasks>

<verification>
- `npm test` — all 5 tests pass (tests mock db, so driver change is transparent)
- `npm run build` — clean build with no import errors
- `npx drizzle-kit push` — schema applied to Supabase (users table exists)
- `grep -q "neondatabase" package.json` should return exit code 1 (removed)
- `grep -q "postgres" package.json` should return exit code 0 (added)
</verification>

<success_criteria>
- src/db/index.ts uses postgres-js + drizzle-orm/postgres-js
- @neondatabase/serverless fully removed from package.json and node_modules
- DATABASE_URL in .env.local points to Supabase
- Schema pushed to Supabase (users table exists)
- All tests green, build clean
</success_criteria>

<output>
After completion, create `.planning/quick/260402-o51-migrate-database-from-neon-to-supabase/260402-o51-SUMMARY.md`
</output>

# Quick Task 260402-o51: Migrate database from Neon to Supabase

**Date:** 2026-04-03
**Commit:** ccb14e5

## What was done

### Task 1 — Driver swap (complete, committed)

- Removed `@neondatabase/serverless` from dependencies
- Added `postgres` (postgres-js) driver
- Rewrote `src/db/index.ts` from `drizzle-orm/neon-http` to `drizzle-orm/postgres-js`
- All 24 tests pass (DB is mocked in tests — driver change is transparent)

### Task 2 — CONNECTION_URL + schema push (partially complete)

- Updated `.env.local` with Supabase pooler connection string template
- Schema push to Supabase **requires manual steps** (see below) — direct connection is IPv6-only and not reachable from dev machine, MCP tools lack project permissions

## Manual steps required

### 1. Apply schema to Supabase

Open the **Supabase SQL Editor** for project `qxhlwgpobiveezhvsxqx` and run:

```sql
CREATE TABLE IF NOT EXISTS users (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  name text NOT NULL,
  linkedin_url text NOT NULL,
  email text,
  session_id uuid NOT NULL UNIQUE,
  created_at timestamp NOT NULL DEFAULT now()
);
```

### 2. Update DATABASE_URL in .env.local

Copy the exact connection string from: **Supabase Dashboard → Project Settings → Database → Connection string → URI**

Replace the placeholder in `.env.local`:
```
DATABASE_URL=postgresql://postgres.qxhlwgpobiveezhvsxqx:YOUR_PASSWORD@aws-0-REGION.pooler.supabase.com:6543/postgres
```

## Status

- [x] `src/db/index.ts` uses `drizzle-orm/postgres-js`
- [x] `@neondatabase/serverless` removed
- [x] 24 tests pass
- [ ] Schema applied to Supabase (manual SQL Editor step)
- [ ] Correct DATABASE_URL confirmed in `.env.local`

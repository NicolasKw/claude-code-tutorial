---
phase: 03-tutorial-content-7-levels
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/lib/content/levels.ts
autonomous: true
requirements: [CONT-01, CONT-04, CONT-05]

must_haves:
  truths:
    - "Levels 0-3 each have 3-5 steps with real terminal commands the user runs"
    - "Every step includes at least one error callout with trigger, error, and solution"
    - "Level content follows the specified progression: Chatbot, Plan Mode, CLAUDE.md, Commands/Skills/Hooks"
  artifacts:
    - path: "src/lib/content/levels.ts"
      provides: "Level content data for levels 0-3"
      contains: "export const LEVEL_CONTENT"
      exports: ["LEVEL_CONTENT", "getLevelContent"]
  key_links:
    - from: "src/lib/content/levels.ts"
      to: "src/lib/types/tutorial.ts"
      via: "import type { LevelContent }"
      pattern: "import.*LevelContent.*from.*tutorial"
---

<objective>
Author tutorial content for Levels 0-3 (Chatbot, Plan Mode, CLAUDE.md, Commands/Skills/Hooks).

Purpose: Create the first half of tutorial content — the foundational Claude Code concepts users learn while building their personal management bot.
Output: `src/lib/content/levels.ts` with complete content for levels 0-3 (levels 4-6 added by Plan 03 in parallel).
</objective>

<execution_context>
@/Users/nicolaskw/Documents/ZalesMachine/Projects/claude-code-tutorial-lead-magnet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolaskw/Documents/ZalesMachine/Projects/claude-code-tutorial-lead-magnet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-tutorial-content-7-levels/03-RESEARCH.md
@.planning/phases/03-tutorial-content-7-levels/03-01-SUMMARY.md

<interfaces>
<!-- Types created in Plan 01 -->
From src/lib/types/tutorial.ts (after Plan 01):
```typescript
export interface ErrorCalloutContent {
  trigger: string;
  error: string;
  solution: string;
}

export interface StepContent {
  title: string;
  explanation: string;
  codeBlock?: {
    code: string;
    language: string;
  };
  errorCallouts: ErrorCalloutContent[];
}

export interface LevelContent {
  level: number;        // 0-6
  title: string;
  subtitle: string;
  summary: string;      // shown in AchievementOverlay
  steps: StepContent[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create levels.ts with content for Levels 0-3</name>
  <files>src/lib/content/levels.ts</files>
  <read_first>
    - src/lib/types/tutorial.ts
    - src/components/tutorial/LevelPage.tsx
    - src/components/tutorial/CodeBlock.tsx
    - .planning/phases/03-tutorial-content-7-levels/03-RESEARCH.md
  </read_first>
  <action>
CREATE directory `src/lib/content/` if it does not exist.

CREATE `src/lib/content/levels.ts` with the following structure. This file will be extended by Plan 03 with levels 4-6.

IMPORTANT constraints:
- Level numbers are 0-6 (NOT 0-7). The shell validates `0 <= level <= 6`.
- Each level MUST have 3-5 steps (time budget: ~2 min/step, ~8 min/level).
- Every step MUST have at least one `errorCallout` entry (CONT-02).
- Every level MUST have at least one step with a `codeBlock` (CONT-01).
- Every level MUST have a non-empty `summary` string (CONT-03).
- All content is in Spanish (matching the tutorial's language).
- The tutorial project is a "bot de gestion personal" (personal management bot), NOT a LinkedIn post writer.
- For multi-line code strings, use `\n` in regular strings to avoid template literal nesting issues.

```typescript
import type { LevelContent } from '@/lib/types/tutorial';

export const LEVEL_CONTENT: LevelContent[] = [
  // Levels 0-3 authored here; Levels 4-6 appended by Plan 03
];

export function getLevelContent(level: number): LevelContent | undefined {
  return LEVEL_CONTENT.find(l => l.level === level);
}
```

**Level 0: Chatbot** (level: 0)
- title: "Chatbot"
- subtitle: "Tu primera conversacion con Claude Code"
- summary: "Instalaste Claude Code, lo ejecutaste en modo chat, y verificaste que responde. Tenes una herramienta de IA corriendo en tu propia maquina."
- Steps (3):
  1. "Instala Claude Code" — `npm install -g @anthropic-ai/claude-code` (bash). Error callout: EACCES permission denied → use sudo or set npm prefix.
  2. "Autentica con tu cuenta Anthropic" — `claude login` (bash). Error callout: browser does not open → copy URL manually from terminal.
  3. "Inicia una conversacion en modo chat" — `claude\n# Luego escribi: "Hola, podrias ayudarme a organizar mis tareas?"` (bash). Error callout: `command not found: claude` → restart terminal or add npm global bin to PATH.

**Level 1: Plan Mode** (level: 1)
- title: "Plan Mode"
- subtitle: "Planifica antes de construir"
- summary: "Usaste el modo plan de Claude Code para disenar la estructura de tu bot antes de escribir codigo. Tenes un plan concreto que Claude va a ejecutar."
- Steps (4):
  1. "Crea la carpeta de tu proyecto" — `mkdir mi-bot-personal && cd mi-bot-personal` (bash). Error callout: directory already exists → use a different name or remove existing.
  2. "Inicia Claude Code en modo plan" — `claude\n# Luego escribi: /plan Quiero un bot de gestion personal en Node.js que me ayude a organizar tareas, notas y recordatorios` (bash). Error callout: Claude starts coding immediately → type `/plan` at the start of the message to force plan mode.
  3. "Revisa y aprueba el plan" — Explanation: Claude te muestra un plan detallado. Leelo, sugeri cambios si es necesario, y aprobalo cuando estes satisfecho. No hay comando — es interaccion en el chat. codeBlock: none. Error callout: plan seems too complex → ask Claude to simplify: "Simplifica el plan, quiero empezar con lo minimo viable."
  4. "Ejecuta el plan" — Explanation: Una vez aprobado, Claude escribe el codigo automaticamente. Mira como crea archivos y carpetas. codeBlock: none (Claude executes the approved plan in-chat). Error callout: Claude asks for confirmation to create files → type "y" or "yes" to approve each file creation.

**Level 2: CLAUDE.md** (level: 2)
- title: "CLAUDE.md"
- subtitle: "Dale memoria a tu proyecto"
- summary: "Creaste un archivo CLAUDE.md que le dice a Claude Code como trabajar en tu proyecto. Ahora cada sesion arranca con contexto, sin repetir instrucciones."
- Steps (3):
  1. "Crea el archivo CLAUDE.md" — `touch CLAUDE.md` (bash). Then explain what CLAUDE.md is: the project memory file Claude reads on every session start. Error callout: "touch: command not found" (Windows) → use `echo. > CLAUDE.md` or create the file manually.
  2. "Escribe las instrucciones del proyecto" — Open CLAUDE.md and add project context. codeBlock with content to paste into CLAUDE.md:
     ```
     # Mi Bot Personal\n\nEste proyecto es un bot de gestion personal en Node.js.\n\n## Convenciones\n- Usar ES modules (import/export)\n- Archivos en espanol\n- Tests con vitest\n\n## Estructura\n- src/ — codigo fuente\n- tests/ — tests
     ```
     (language: "markdown"). Error callout: Claude does not seem to read CLAUDE.md → make sure the file is in the project root (same directory where you run `claude`).
  3. "Verifica que Claude lee el archivo" — `claude\n# Luego pregunta: "Que sabes sobre este proyecto?"` (bash). Claude should reference the content from CLAUDE.md. Error callout: Claude says "I don't have context" → verify CLAUDE.md is in the current working directory, not in a subdirectory.

**Level 3: Commands, Skills y Hooks** (level: 3)
- title: "Commands, Skills y Hooks"
- subtitle: "Automatiza tareas repetitivas"
- summary: "Configuraste un comando personalizado, un skill file y un hook. Tu proyecto ahora tiene atajos y automatizaciones que aceleran el desarrollo."
- Steps (4):
  1. "Crea un comando personalizado" — `mkdir -p .claude/commands && echo "Revisa el codigo del proyecto y sugeri mejoras de calidad, performance y legibilidad. Genera un reporte con las sugerencias." > .claude/commands/revisar.md` (bash). Explain what custom commands are: shortcuts you can invoke with `/commands/revisar`. Error callout: `.claude/commands` directory not recognized → make sure you are in the project root directory where CLAUDE.md lives.
  2. "Prueba tu comando" — `claude\n# Luego escribi: /commands/revisar` (bash). Error callout: command not found → verify the file is at `.claude/commands/revisar.md` (exact path).
  3. "Crea un skill file" — `mkdir -p .claude/skills && echo "# Skill: Gestionar Tareas\n\nCuando el usuario pide crear, listar o completar tareas, usa el archivo src/tasks.json como almacenamiento. Formato: [{id, titulo, completada, fecha}]" > .claude/skills/tareas.md` (bash). Explain skills: reusable knowledge blocks Claude uses when relevant. Error callout: Claude does not use the skill → mention the skill name explicitly in your prompt: "usa el skill de tareas."
  4. "Configura un hook de pre-commit" — Explain hooks: actions Claude runs automatically at certain points. Create `.claude/settings.json` with a hook config. codeBlock: `echo '{\n  "hooks": {\n    "pre-commit": "npx vitest run --reporter=verbose"\n  }\n}' > .claude/settings.json` (bash). Error callout: hook does not run → confirm settings.json is valid JSON (no trailing commas).

Each level must follow EXACTLY the type signatures from `LevelContent` and `StepContent`. Every step MUST have at least one entry in `errorCallouts` array. Use `\n` for newlines in code strings.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/content.test.ts 2>&1 || echo "Content tests are still todo stubs — verifying file exists and TypeScript compiles"; npx tsc --noEmit src/lib/content/levels.ts 2>&1 || echo "Direct tsc check not available — build check next"; npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/content/levels.ts exists
    - src/lib/content/levels.ts contains `export const LEVEL_CONTENT: LevelContent[]`
    - src/lib/content/levels.ts contains `export function getLevelContent`
    - src/lib/content/levels.ts contains `level: 0` (Level 0 entry)
    - src/lib/content/levels.ts contains `level: 1` (Level 1 entry)
    - src/lib/content/levels.ts contains `level: 2` (Level 2 entry)
    - src/lib/content/levels.ts contains `level: 3` (Level 3 entry)
    - src/lib/content/levels.ts contains `title: 'Chatbot'` or `title: "Chatbot"`
    - src/lib/content/levels.ts contains `title: 'Plan Mode'` or `title: "Plan Mode"`
    - src/lib/content/levels.ts contains `title: 'CLAUDE.md'` or `title: "CLAUDE.md"`
    - src/lib/content/levels.ts contains `title: 'Commands, Skills y Hooks'` or `title: "Commands, Skills y Hooks"`
    - Every level 0-3 entry has a non-empty `summary` string
    - Every level has `steps` array with 3-5 entries
    - Every step has non-empty `errorCallouts` array (at least 1 entry)
    - File compiles without TypeScript errors
  </acceptance_criteria>
  <done>Levels 0-3 fully authored in src/lib/content/levels.ts with typed content, real terminal commands, and error callouts on every step</done>
</task>

</tasks>

<verification>
- `src/lib/content/levels.ts` contains 4 level entries (0-3) with complete step content
- Every step has at least one errorCallout
- Every level has at least one step with a codeBlock
- File imports LevelContent type correctly
- No TypeScript compilation errors
</verification>

<success_criteria>
- Levels 0-3 complete in src/lib/content/levels.ts
- Each level has 3-5 steps within the 2-min-per-step time budget
- Every step has error callouts (CONT-02)
- Every level has at least one command/code block (CONT-01)
- Content is in Spanish and builds a personal management bot (CONT-04)
</success_criteria>

<output>
After completion, create `.planning/phases/03-tutorial-content-7-levels/03-02-SUMMARY.md`
</output>

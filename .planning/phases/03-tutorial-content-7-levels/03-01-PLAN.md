---
phase: 03-tutorial-content-7-levels
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types/tutorial.ts
  - src/components/tutorial/ErrorCallout.tsx
  - src/components/__tests__/ErrorCallout.test.tsx
  - src/lib/__tests__/content.test.ts
autonomous: true
requirements: [CONT-02]

must_haves:
  truths:
    - "ErrorCallout renders a collapsible details/summary block with trigger, error, and solution text"
    - "Test stubs exist for all CONT requirements and fail or are pending (Wave 0 contract)"
  artifacts:
    - path: "src/lib/types/tutorial.ts"
      provides: "ErrorCalloutContent, StepContent, LevelContent interfaces"
      contains: "export interface LevelContent"
    - path: "src/components/tutorial/ErrorCallout.tsx"
      provides: "Collapsible error callout component"
      exports: ["ErrorCallout"]
    - path: "src/components/__tests__/ErrorCallout.test.tsx"
      provides: "Unit tests for ErrorCallout"
      contains: "describe('ErrorCallout'"
    - path: "src/lib/__tests__/content.test.ts"
      provides: "Content validation test stubs"
      contains: "describe('LEVEL_CONTENT'"
  key_links:
    - from: "src/components/tutorial/ErrorCallout.tsx"
      to: "src/lib/types/tutorial.ts"
      via: "import type { ErrorCalloutContent }"
      pattern: "import.*ErrorCalloutContent.*from.*tutorial"
---

<objective>
Create content type definitions, the ErrorCallout component, and Wave 0 test stubs for Phase 3.

Purpose: Establish contracts (types) and test infrastructure before content authoring begins.
Output: Extended tutorial types, working ErrorCallout component, test stubs for CONT-01 through CONT-04.
</objective>

<execution_context>
@/Users/nicolaskw/Documents/ZalesMachine/Projects/claude-code-tutorial-lead-magnet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolaskw/Documents/ZalesMachine/Projects/claude-code-tutorial-lead-magnet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tutorial-content-7-levels/03-RESEARCH.md

<interfaces>
<!-- Existing types in src/lib/types/tutorial.ts -->
From src/lib/types/tutorial.ts:
```typescript
export interface TutorialProgress {
  currentLevel: number;
  completedLevels: number[];
  completedSteps: Record<number, number[]>;
}

export const PROGRESS_KEY = 'tutorial_progress';
export const TOTAL_LEVELS = 7;
export const DEFAULT_PROGRESS: TutorialProgress = {
  currentLevel: 0,
  completedLevels: [],
  completedSteps: {},
};
```

From src/components/tutorial/CodeBlock.tsx:
```typescript
interface CodeBlockProps {
  code: string;
  language?: string;
}
export function CodeBlock({ code, language }: CodeBlockProps) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add content types to tutorial.ts and create ErrorCallout component</name>
  <files>src/lib/types/tutorial.ts, src/components/tutorial/ErrorCallout.tsx, src/components/__tests__/ErrorCallout.test.tsx</files>
  <read_first>
    - src/lib/types/tutorial.ts
    - src/components/tutorial/CodeBlock.tsx
    - src/components/tutorial/StepCard.tsx
  </read_first>
  <behavior>
    - Test: ErrorCallout renders a details element
    - Test: ErrorCallout summary contains trigger text and error text
    - Test: ErrorCallout expanded content contains solution text
    - Test: ErrorCallout has amber background styling (bg-amber-50 class)
  </behavior>
  <action>
1. EXTEND `src/lib/types/tutorial.ts` — append these interfaces AFTER the existing exports (do NOT remove existing TutorialProgress, PROGRESS_KEY, TOTAL_LEVELS, DEFAULT_PROGRESS):

```typescript
export interface ErrorCalloutContent {
  trigger: string;    // e.g. "Si ves este error"
  error: string;      // the error message / condition
  solution: string;   // what to do to fix it
}

export interface StepContent {
  title: string;
  explanation: string;
  codeBlock?: {
    code: string;
    language: string;
  };
  errorCallouts: ErrorCalloutContent[];
}

export interface LevelContent {
  level: number;        // 0-6
  title: string;        // e.g. "Chatbot"
  subtitle: string;     // e.g. "Tu primera conversacion con Claude Code"
  summary: string;      // shown in AchievementOverlay "Que lograste" card
  steps: StepContent[];
}
```

2. CREATE `src/components/__tests__/ErrorCallout.test.tsx` with tests:
- Renders a `<details>` element
- `<summary>` contains the trigger and error text joined by " — "
- The paragraph inside details contains the solution text
- The details element has class `bg-amber-50`

3. CREATE `src/components/tutorial/ErrorCallout.tsx` — NO 'use client' directive (native details/summary needs no React state):

```tsx
import type { ErrorCalloutContent } from '@/lib/types/tutorial';

export function ErrorCallout({ trigger, error, solution }: ErrorCalloutContent) {
  return (
    <details className="mt-3 rounded-md border border-amber-200 bg-amber-50 p-3">
      <summary className="cursor-pointer text-sm font-semibold text-amber-800 select-none">
        {trigger} — {error}
      </summary>
      <p className="mt-2 text-sm text-amber-900 leading-relaxed">{solution}</p>
    </details>
  );
}
```

4. Run tests to confirm GREEN.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/ErrorCallout.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/types/tutorial.ts contains `export interface ErrorCalloutContent`
    - src/lib/types/tutorial.ts contains `export interface StepContent`
    - src/lib/types/tutorial.ts contains `export interface LevelContent`
    - src/lib/types/tutorial.ts still contains `export interface TutorialProgress`
    - src/lib/types/tutorial.ts still contains `export const TOTAL_LEVELS = 7`
    - src/components/tutorial/ErrorCallout.tsx contains `export function ErrorCallout`
    - src/components/tutorial/ErrorCallout.tsx does NOT contain `'use client'`
    - src/components/tutorial/ErrorCallout.tsx contains `<details`
    - src/components/tutorial/ErrorCallout.tsx contains `bg-amber-50`
    - src/components/__tests__/ErrorCallout.test.tsx exits 0
  </acceptance_criteria>
  <done>ErrorCallout component renders collapsible error/solution block; all new types exported from tutorial.ts; ErrorCallout tests GREEN</done>
</task>

<task type="auto">
  <name>Task 2: Create Wave 0 content validation test stubs</name>
  <files>src/lib/__tests__/content.test.ts</files>
  <read_first>
    - src/lib/types/tutorial.ts
  </read_first>
  <action>
CREATE `src/lib/__tests__/content.test.ts` — these test stubs validate the LEVEL_CONTENT data shape that Plans 02/03 will populate. Use `it.todo()` for tests that cannot pass yet (LEVEL_CONTENT does not exist yet).

```typescript
import { describe, it, expect } from 'vitest';

describe('LEVEL_CONTENT', () => {
  it.todo('exports exactly 7 level entries (levels 0-6) — CONT-04');

  it.todo('every level has a non-empty title and subtitle — CONT-04');

  it.todo('every level has a non-empty summary string — CONT-03');

  it.todo('every level has between 3 and 5 steps — CONT-05 time budget');

  it.todo('every step has a non-empty title and explanation — CONT-01');

  it.todo('every level has at least one step with a codeBlock — CONT-01');

  it.todo('every step has at least one errorCallout — CONT-02');

  it.todo('every errorCallout has non-empty trigger, error, and solution — CONT-02');

  it.todo('level numbers are sequential 0 through 6 — CONT-04');
});

describe('getLevelContent', () => {
  it.todo('returns the correct level by number');

  it.todo('returns undefined for level 7 (out of bounds)');
});
```

This file establishes the test contract. Plan 04 (wiring) will convert these to real assertions.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/content.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/__tests__/content.test.ts contains `describe('LEVEL_CONTENT'`
    - src/lib/__tests__/content.test.ts contains `it.todo('exports exactly 7 level entries`
    - src/lib/__tests__/content.test.ts contains `CONT-01`
    - src/lib/__tests__/content.test.ts contains `CONT-02`
    - src/lib/__tests__/content.test.ts contains `CONT-03`
    - src/lib/__tests__/content.test.ts contains `CONT-04`
    - src/lib/__tests__/content.test.ts contains `CONT-05`
    - npx vitest run src/lib/__tests__/content.test.ts exits 0 (all todos are pending, not failing)
  </acceptance_criteria>
  <done>Wave 0 content test stubs exist covering CONT-01 through CONT-05; all pending (it.todo); suite exits 0</done>
</task>

</tasks>

<verification>
- `npx vitest run src/components/__tests__/ErrorCallout.test.tsx` — all tests GREEN
- `npx vitest run src/lib/__tests__/content.test.ts` — exits 0 (all pending)
- `npx vitest run` — full suite still passes (no regressions)
</verification>

<success_criteria>
- ErrorCallout component exists and renders collapsible details/summary
- Content type interfaces (ErrorCalloutContent, StepContent, LevelContent) exported from tutorial.ts
- Wave 0 test stubs cover all CONT requirements
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-tutorial-content-7-levels/03-01-SUMMARY.md`
</output>

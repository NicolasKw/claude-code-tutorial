---
phase: 03-tutorial-content-7-levels
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - src/lib/content/levels.ts
autonomous: true
requirements: [CONT-01, CONT-04, CONT-05]

must_haves:
  truths:
    - "Levels 4-6 each have 3-5 steps with real terminal commands the user runs"
    - "Every step includes at least one error callout with trigger, error, and solution"
    - "Level content follows the specified progression: MCP Servers, GSD Framework, Sub-agentes + Flujos autonomos"
    - "LEVEL_CONTENT array has exactly 7 entries covering levels 0-6"
  artifacts:
    - path: "src/lib/content/levels.ts"
      provides: "Complete level content data for all 7 levels (0-6)"
      contains: "level: 6"
      exports: ["LEVEL_CONTENT", "getLevelContent"]
  key_links:
    - from: "src/lib/content/levels.ts"
      to: "src/lib/types/tutorial.ts"
      via: "import type { LevelContent }"
      pattern: "import.*LevelContent.*from.*tutorial"
---

<objective>
Author tutorial content for Levels 4-6 (MCP Servers, GSD Framework, Sub-agentes + Flujos autonomos).

Purpose: Complete the second half of tutorial content — the advanced Claude Code concepts. Note: the ROADMAP lists 8 named levels (Nivel 0-7) but the shell supports levels 0-6 (TOTAL_LEVELS=7). "Nivel 6: Sub-agentes" and "Nivel 7: Flujos autonomos" are combined into level index 6.
Output: Updated `src/lib/content/levels.ts` with all 7 levels complete.
</objective>

<execution_context>
@/Users/nicolaskw/Documents/ZalesMachine/Projects/claude-code-tutorial-lead-magnet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolaskw/Documents/ZalesMachine/Projects/claude-code-tutorial-lead-magnet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-tutorial-content-7-levels/03-RESEARCH.md
@.planning/phases/03-tutorial-content-7-levels/03-01-SUMMARY.md
@.planning/phases/03-tutorial-content-7-levels/03-02-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 -->
From src/lib/types/tutorial.ts:
```typescript
export interface ErrorCalloutContent {
  trigger: string;
  error: string;
  solution: string;
}

export interface StepContent {
  title: string;
  explanation: string;
  codeBlock?: {
    code: string;
    language: string;
  };
  errorCallouts: ErrorCalloutContent[];
}

export interface LevelContent {
  level: number;        // 0-6
  title: string;
  subtitle: string;
  summary: string;
  steps: StepContent[];
}
```

From src/lib/content/levels.ts (after Plan 02):
```typescript
export const LEVEL_CONTENT: LevelContent[] = [
  // Levels 0-3 already populated by Plan 02
  // Levels 4-6 to be appended here
];

export function getLevelContent(level: number): LevelContent | undefined {
  return LEVEL_CONTENT.find(l => l.level === level);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append content for Levels 4-6 to levels.ts</name>
  <files>src/lib/content/levels.ts</files>
  <read_first>
    - src/lib/content/levels.ts
    - src/lib/types/tutorial.ts
    - .planning/phases/03-tutorial-content-7-levels/03-RESEARCH.md
  </read_first>
  <action>
MODIFY `src/lib/content/levels.ts` — append levels 4, 5, and 6 to the existing `LEVEL_CONTENT` array AFTER the level 3 entry. Do NOT remove or modify levels 0-3.

IMPORTANT constraints:
- Level 6 is the FINAL level (shell validates `0 <= level <= 6`). It combines "Sub-agentes" and "Flujos autonomos" topics.
- Each level MUST have 3-5 steps.
- Every step MUST have at least one `errorCallout` entry.
- Every level MUST have at least one step with a `codeBlock`.
- Every level MUST have a non-empty `summary`.
- All content in Spanish. Bot project is "bot de gestion personal."
- After appending, `LEVEL_CONTENT.length` MUST equal 7.

**Level 4: MCP Servers** (level: 4)
- title: "MCP Servers"
- subtitle: "Conecta herramientas externas"
- summary: "Conectaste un servidor MCP a Claude Code. Tu bot ahora puede acceder a herramientas externas — leer archivos, hacer requests HTTP, y mas — sin que vos escribas el codigo de integracion."
- Steps (3):
  1. "Entiende que es un MCP Server" — Explanation: MCP (Model Context Protocol) permite que Claude Code use herramientas externas como si fueran nativas. Un MCP server expone herramientas que Claude puede invocar. No code block for this step (explanation only). Error callout: "Parece complicado" → "MCP es solo un protocolo de comunicacion. Pensa en los MCP servers como plugins que le dan superpoderes a Claude."
  2. "Configura el MCP server de filesystem" — codeBlock: `claude mcp add filesystem -- npx -y @anthropic-ai/mcp-filesystem --dir .` (bash). Explain: this gives Claude Code the ability to read/write files through MCP tools. Error callout: "Error: MCP server failed to start" → verify Node.js version is 18+ with `node --version`. MCP servers require Node 18 or later.
  3. "Usa el MCP server en tu bot" — `claude\n# Luego escribi: "Usa el MCP de filesystem para listar todos los archivos del proyecto y decime cuales son de configuracion"` (bash). Explain: Claude now uses the MCP tool instead of its built-in file access. Error callout: "Claude does not use MCP tools" → verify the server is configured with `claude mcp list` — it should show "filesystem" in the list.

**Level 5: GSD Framework** (level: 5)
- title: "GSD Framework"
- subtitle: "Estructura tu trabajo con Claude"
- summary: "Implementaste el framework GSD (Get Shit Done) en tu proyecto. Ahora tenes un flujo estructurado para planificar, ejecutar y verificar tareas con Claude Code."
- Steps (4):
  1. "Instala el framework GSD" — codeBlock: `claude\n# Luego escribi: "Instala el framework GSD en este proyecto. Crea la estructura de carpetas .claude/get-shit-done/ con los workflows basicos."` (bash). Explain: GSD is a structured workflow that gives Claude Code a repeatable process: plan, execute, verify. Error callout: "Claude does not know what GSD is" → describe it explicitly: "GSD es un framework de workflows para Claude Code que organiza el trabajo en fases: planificar, ejecutar, verificar."
  2. "Crea tu primer plan con GSD" — codeBlock: `claude\n# Luego escribi: "/gsd:plan Quiero agregar un sistema de recordatorios a mi bot personal"` (bash). Explain: the /gsd:plan command creates a structured plan file that Claude can then execute step by step. Error callout: "/gsd:plan command not recognized" → make sure you installed GSD in the previous step. Check that `.claude/get-shit-done/` directory exists.
  3. "Ejecuta el plan" — codeBlock: `claude\n# Luego escribi: "/gsd:execute"` (bash). Explain: Claude reads the plan and executes each task in order, committing progress. Error callout: "Claude skips tasks or does them out of order" → the plan file may have unclear task dependencies. Review the plan and ask Claude to re-plan if needed.
  4. "Verifica el resultado" — codeBlock: `claude\n# Luego escribi: "/gsd:verify"` (bash). Explain: verification checks that the plan was executed correctly and all acceptance criteria are met. Error callout: "Verification fails" → read the verification output — it tells you exactly what is missing. Fix the issues and run verify again.

**Level 6: Sub-agentes y Flujos Autonomos** (level: 6)
- title: "Sub-agentes y Flujos Autonomos"
- subtitle: "Delega y automatiza"
- summary: "Configuraste sub-agentes y un flujo autonomo completo. Tu bot de gestion personal ahora puede ejecutar tareas complejas en multiples pasos sin intervencion manual. Completaste el tutorial — sos un usuario avanzado de Claude Code."
- Steps (5):
  1. "Entiende los sub-agentes" — Explanation: Sub-agents are Claude Code instances that the main agent can spawn to handle specific subtasks. Each sub-agent gets its own context and can work independently. No codeBlock. Error callout: "Cuando uso sub-agentes?" → "Cuando una tarea tiene partes independientes que se pueden hacer en paralelo — por ejemplo, actualizar tests mientras se refactoriza codigo."
  2. "Crea un workflow con sub-agentes" — codeBlock: `claude\n# Luego escribi: "Crea un workflow que use sub-agentes para: 1) revisar la calidad del codigo, 2) actualizar los tests, y 3) mejorar la documentacion. Los tres pueden correr en paralelo."` (bash). Explain: Claude will create agent definitions and coordinate them. Error callout: "Claude does everything in sequence instead of parallel" → be explicit: "Usa Task tool para crear sub-agentes que trabajen en paralelo, no en secuencia."
  3. "Configura un flujo autonomo" — codeBlock: `claude\n# Luego escribi: "Configura un flujo autonomo que cada vez que le pida 'organizar mi dia', lea mis tareas pendientes, priorice por urgencia, y genere un plan del dia en formato markdown."` (bash). Explain: autonomous flows run with minimal interaction — you give a trigger and Claude handles the rest. Error callout: "Claude asks too many questions instead of acting autonomously" → add to CLAUDE.md: "Cuando el usuario pide organizar el dia, actua sin pedir confirmacion."
  4. "Ejecuta el flujo completo" — codeBlock: `claude\n# Luego escribi: "Organiza mi dia"` (bash). Explain: watch Claude execute the full flow — read tasks, prioritize, generate plan — without asking for permission at each step. Error callout: "The output is not what I expected" → refine the CLAUDE.md instructions and try again. Autonomous flows improve with better context.
  5. "Revisa lo que construiste" — Explanation: Mira la carpeta de tu proyecto. Tenes un bot de gestion personal con: modo chat, planificacion, memoria de proyecto (CLAUDE.md), comandos personalizados, skills, hooks, MCP servers, flujos GSD, sub-agentes y automatizacion. Todo corriendo en tu maquina. No codeBlock. Error callout: "No puedo creer que hice todo esto" → "Es real. Estos son los mismos patrones que usan los desarrolladores profesionales con Claude Code. Ahora sos uno de ellos."

After appending, verify: `LEVEL_CONTENT.length === 7` and levels go 0, 1, 2, 3, 4, 5, 6.
  </action>
  <verify>
    <automated>node -e "const m = require('./src/lib/content/levels.ts'); console.log(m.LEVEL_CONTENT.length)" 2>&1 || echo "Cannot require TS directly — checking with grep"; grep -c "level:" src/lib/content/levels.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/content/levels.ts contains `level: 4` (Level 4 entry)
    - src/lib/content/levels.ts contains `level: 5` (Level 5 entry)
    - src/lib/content/levels.ts contains `level: 6` (Level 6 entry)
    - src/lib/content/levels.ts contains `title: 'MCP Servers'` or `title: "MCP Servers"`
    - src/lib/content/levels.ts contains `title: 'GSD Framework'` or `title: "GSD Framework"`
    - src/lib/content/levels.ts contains `title: 'Sub-agentes y Flujos Autonomos'` or `title: "Sub-agentes y Flujos Autonomos"`
    - src/lib/content/levels.ts does NOT contain `level: 7` (no level 7)
    - Every level 4-6 entry has a non-empty `summary` string
    - Every level has `steps` array with 3-5 entries
    - Every step has non-empty `errorCallouts` array (at least 1 entry)
    - Levels 0-3 from Plan 02 are unchanged
    - File still compiles without TypeScript errors
  </acceptance_criteria>
  <done>All 7 levels (0-6) fully authored in src/lib/content/levels.ts with typed content, real commands, and error callouts on every step</done>
</task>

</tasks>

<verification>
- `src/lib/content/levels.ts` contains exactly 7 level entries (0-6)
- Levels 4-6 have complete step content
- Every step has at least one errorCallout
- Every level has at least one step with a codeBlock
- No level: 7 entry exists
</verification>

<success_criteria>
- Levels 4-6 complete in src/lib/content/levels.ts
- Each level has 3-5 steps within the 2-min-per-step time budget
- Every step has error callouts (CONT-02)
- Every level has at least one command/code block (CONT-01)
- Content covers MCP Servers, GSD Framework, Sub-agentes + Flujos autonomos (CONT-04)
- Total: 7 levels, all with content
</success_criteria>

<output>
After completion, create `.planning/phases/03-tutorial-content-7-levels/03-03-SUMMARY.md`
</output>

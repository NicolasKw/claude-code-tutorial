---
phase: 02-tutorial-shell-progress-engine
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/__tests__/progress.test.ts
  - src/hooks/__tests__/useProgress.test.ts
  - src/components/__tests__/TutorialHeader.test.tsx
  - src/components/__tests__/LevelPage.test.tsx
  - src/components/__tests__/StepCard.test.tsx
  - src/components/__tests__/AchievementOverlay.test.tsx
  - src/components/__tests__/CodeBlock.test.tsx
  - src/app/actions/__tests__/progress.test.ts
autonomous: true
requirements:
  - TUTO-01
  - TUTO-02
  - TUTO-03
  - TUTO-04
  - TUTO-05
  - TUTO-06

must_haves:
  truths:
    - "All 8 test stub files exist and are parseable by Vitest"
    - "Running npm test reports 8 test suites with all tests in todo/skip state"
    - "No test passes — all are RED stubs awaiting implementation in Wave 2"
  artifacts:
    - path: "src/lib/__tests__/progress.test.ts"
      provides: "Test stubs for isLevelLocked, completeStep, completeLevel, isStepCompleted"
      contains: "isLevelLocked"
    - path: "src/hooks/__tests__/useProgress.test.ts"
      provides: "Test stubs for useProgress hook"
      contains: "useProgress"
    - path: "src/components/__tests__/TutorialHeader.test.tsx"
      provides: "Test stubs for TutorialHeader sticky header"
      contains: "Nivel"
    - path: "src/components/__tests__/LevelPage.test.tsx"
      provides: "Test stubs for level lock gate"
      contains: "bloqueado"
    - path: "src/components/__tests__/StepCard.test.tsx"
      provides: "Test stubs for step completion"
      contains: "Listo, ya lo hice"
    - path: "src/components/__tests__/AchievementOverlay.test.tsx"
      provides: "Test stubs for achievement overlay"
      contains: "completado"
    - path: "src/components/__tests__/CodeBlock.test.tsx"
      provides: "Test stubs for code copy"
      contains: "clipboard"
    - path: "src/app/actions/__tests__/progress.test.ts"
      provides: "Test stubs for saveProgress server action"
      contains: "saveProgress"
  key_links: []
---

<objective>
Create all 8 failing test stub files for Phase 2 before any implementation begins.

Purpose: Establish the Nyquist sampling contract — every behavioral requirement has a corresponding test waiting to go green. This forces Wave 1 and Wave 2 executors to write code that satisfies specific, pre-defined expectations.

Output: 8 test files with `it.todo()` or `describe` + pending test skeletons. All tests should be recognized by Vitest but none should pass (RED state).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tutorial-shell-progress-engine/02-VALIDATION.md
@.planning/phases/02-tutorial-shell-progress-engine/02-UI-SPEC.md
@.planning/phases/02-tutorial-shell-progress-engine/02-RESEARCH.md

<interfaces>
<!-- Existing test infrastructure the executor needs -->

From vitest.config.ts:
```typescript
import { defineConfig } from 'vitest/config';
import path from 'path';

export default defineConfig({
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./src/test-setup.ts'],
    include: ['src/**/*.test.{ts,tsx}'],
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
});
```

From src/test-setup.ts (localStorage mock already provided):
```typescript
// Custom localStorage mock for jsdom — already available in all tests
```

Existing test pattern (from src/lib/__tests__/session.test.ts):
```typescript
import { describe, it, expect, beforeEach } from 'vitest';
// Tests use describe/it/expect from vitest with globals: true
```

Existing component test pattern (from src/components/__tests__/SessionGuard.test.tsx):
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { render, screen, waitFor } from '@testing-library/react';
// Mock modules with vi.mock()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pure logic and hook test stubs (4 files)</name>
  <files>src/lib/__tests__/progress.test.ts, src/hooks/__tests__/useProgress.test.ts, src/app/actions/__tests__/progress.test.ts</files>
  <read_first>
    - src/lib/__tests__/session.test.ts (existing test pattern — follow same import style)
    - src/app/actions/__tests__/register.test.ts (existing server action test pattern)
    - .planning/phases/02-tutorial-shell-progress-engine/02-VALIDATION.md (exact test requirements)
    - .planning/phases/02-tutorial-shell-progress-engine/02-RESEARCH.md (Pattern 1: useProgress hook shape, Pattern 3: saveProgress signature)
  </read_first>
  <action>
    Create 3 test stub files. Use `it.todo()` for each test case — these must NOT pass, only register as pending in Vitest.

    **File 1: `src/lib/__tests__/progress.test.ts`**
    ```typescript
    import { describe, it } from 'vitest';

    describe('isLevelLocked', () => {
      it.todo('returns false for level 0 (always unlocked)');
      it.todo('returns false when level equals currentLevel');
      it.todo('returns true when level > currentLevel');
      it.todo('returns false when level < currentLevel');
    });

    describe('isStepCompleted', () => {
      it.todo('returns true when step is in completedSteps for the level');
      it.todo('returns false when step is not in completedSteps');
      it.todo('returns false when level has no completedSteps entry');
    });

    describe('completeStep', () => {
      it.todo('adds step to completedSteps for the given level');
      it.todo('does not duplicate if step already completed');
      it.todo('returns a new object (no mutation)');
    });

    describe('completeLevel', () => {
      it.todo('adds level to completedLevels');
      it.todo('advances currentLevel to level + 1');
      it.todo('does not duplicate if level already in completedLevels');
      it.todo('returns a new object (no mutation)');
    });
    ```

    **File 2: `src/hooks/__tests__/useProgress.test.ts`**
    Create the `src/hooks/__tests__/` directory first.
    ```typescript
    import { describe, it } from 'vitest';

    describe('useProgress', () => {
      it.todo('initializes with DEFAULT_PROGRESS when localStorage is empty');
      it.todo('reads existing progress from localStorage on mount');
      it.todo('completeStep updates localStorage with new step');
      it.todo('completeLevel advances currentLevel and updates localStorage');
      it.todo('completeLevel calls saveProgress server action with sessionId');
      it.todo('isLevelLocked returns true for levels beyond currentLevel');
      it.todo('isStepCompleted returns true for completed steps');
    });
    ```

    **File 3: `src/app/actions/__tests__/progress.test.ts`**
    ```typescript
    import { describe, it } from 'vitest';

    describe('saveProgress', () => {
      it.todo('looks up user by sessionId');
      it.todo('returns { success: false } when user not found');
      it.todo('upserts progress data with onConflictDoUpdate');
      it.todo('returns { success: true } on successful save');
    });
    ```

    IMPORTANT: Use `it.todo('description')` — not `it.skip` or `it('desc', () => {})`. The `it.todo` pattern is the Vitest standard for pending tests that show as "todo" in output, not as failures or passes.
  </action>
  <verify>
    <automated>test -f src/lib/__tests__/progress.test.ts && test -f src/hooks/__tests__/useProgress.test.ts && test -f src/app/actions/__tests__/progress.test.ts && grep -c "it.todo" src/lib/__tests__/progress.test.ts && grep -c "it.todo" src/hooks/__tests__/useProgress.test.ts && grep -c "it.todo" src/app/actions/__tests__/progress.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/__tests__/progress.test.ts` contains 14 `it.todo` calls across 4 describe blocks
    - `src/hooks/__tests__/useProgress.test.ts` contains 7 `it.todo` calls in 1 describe block
    - `src/app/actions/__tests__/progress.test.ts` contains 4 `it.todo` calls in 1 describe block
    - All files use `import { describe, it } from 'vitest'` (no expect needed for todos)
    - `grep -c "it.todo" src/lib/__tests__/progress.test.ts` returns 14
    - `grep -c "it.todo" src/hooks/__tests__/useProgress.test.ts` returns 7
    - `grep -c "it.todo" src/app/actions/__tests__/progress.test.ts` returns 4
  </acceptance_criteria>
  <done>3 test stub files created with 25 total it.todo tests covering pure logic, hook, and server action</done>
</task>

<task type="auto">
  <name>Task 2: Create component test stubs (5 files)</name>
  <files>src/components/__tests__/TutorialHeader.test.tsx, src/components/__tests__/LevelPage.test.tsx, src/components/__tests__/StepCard.test.tsx, src/components/__tests__/AchievementOverlay.test.tsx, src/components/__tests__/CodeBlock.test.tsx</files>
  <read_first>
    - src/components/__tests__/SessionGuard.test.tsx (existing component test pattern — follow same mock style)
    - .planning/phases/02-tutorial-shell-progress-engine/02-UI-SPEC.md (exact copy strings, component specs)
    - .planning/phases/02-tutorial-shell-progress-engine/02-VALIDATION.md (Wave 0 requirements for each component)
  </read_first>
  <action>
    Create 5 component test stub files. Use `.tsx` extension since these will eventually render React components.

    **File 1: `src/components/__tests__/TutorialHeader.test.tsx`** (TUTO-06)
    ```typescript
    import { describe, it } from 'vitest';

    describe('TutorialHeader', () => {
      it.todo('renders "Nivel X de 7" badge with current level number');
      it.todo('renders progress bar with correct aria-valuenow based on currentLevel');
      it.todo('progress bar aria-valuemin is 0 and aria-valuemax is 100');
      it.todo('progress bar has aria-label "Progreso del tutorial"');
      it.todo('header has sticky positioning class');
    });
    ```

    **File 2: `src/components/__tests__/LevelPage.test.tsx`** (TUTO-01)
    ```typescript
    import { describe, it } from 'vitest';

    describe('LevelPage', () => {
      it.todo('renders level content when level is unlocked');
      it.todo('renders LockedLevel with "Este nivel está bloqueado" when level is locked');
      it.todo('renders "Completá el Nivel X para desbloquearlo." with correct level number');
      it.todo('renders "Volver al Nivel X" button linking to current unlocked level');
      it.todo('renders lock icon when level is locked');
    });
    ```

    **File 3: `src/components/__tests__/StepCard.test.tsx`** (TUTO-03)
    ```typescript
    import { describe, it } from 'vitest';

    describe('StepCard', () => {
      it.todo('renders step content');
      it.todo('renders "Listo, ya lo hice" button when step is not completed');
      it.todo('clicking "Listo, ya lo hice" calls onComplete callback');
      it.todo('shows completed state with Check icon when step is done');
      it.todo('completed button shows "Listo, ya lo hice" text with disabled style');
      it.todo('calls onLevelComplete when final step is completed');
    });
    ```

    **File 4: `src/components/__tests__/AchievementOverlay.test.tsx`** (TUTO-02)
    ```typescript
    import { describe, it } from 'vitest';

    describe('AchievementOverlay', () => {
      it.todo('renders nothing when show is false');
      it.todo('renders overlay with "Nivel X completado" heading when show is true');
      it.todo('renders "Qué lograste" subheading');
      it.todo('renders "Ir al Nivel X+1" CTA button for non-final levels');
      it.todo('renders "Ver mi certificado" CTA button for level 7');
      it.todo('has role="dialog" and aria-modal="true"');
      it.todo('CTA button click calls onNavigate callback');
    });
    ```

    **File 5: `src/components/__tests__/CodeBlock.test.tsx`** (TUTO-05)
    ```typescript
    import { describe, it } from 'vitest';

    describe('CodeBlock', () => {
      it.todo('renders code content in monospace font');
      it.todo('renders copy button with Copy icon');
      it.todo('clicking copy button calls navigator.clipboard.writeText with code content');
      it.todo('shows Check icon for 2 seconds after successful copy');
      it.todo('copy button has aria-label "Copiar código"');
      it.todo('after copy, aria-label changes to "Código copiado"');
    });
    ```

    IMPORTANT: Use `it.todo('description')` for all tests. These are stubs — no assertions, no renders, no imports beyond vitest.
  </action>
  <verify>
    <automated>test -f src/components/__tests__/TutorialHeader.test.tsx && test -f src/components/__tests__/LevelPage.test.tsx && test -f src/components/__tests__/StepCard.test.tsx && test -f src/components/__tests__/AchievementOverlay.test.tsx && test -f src/components/__tests__/CodeBlock.test.tsx && npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 5 `.test.tsx` files exist in `src/components/__tests__/`
    - `grep -c "it.todo" src/components/__tests__/TutorialHeader.test.tsx` returns 5
    - `grep -c "it.todo" src/components/__tests__/LevelPage.test.tsx` returns 5
    - `grep -c "it.todo" src/components/__tests__/StepCard.test.tsx` returns 6
    - `grep -c "it.todo" src/components/__tests__/AchievementOverlay.test.tsx` returns 7
    - `grep -c "it.todo" src/components/__tests__/CodeBlock.test.tsx` returns 6
    - `npm test` runs all 8 new test suites (+ existing suites) without errors
    - All new tests show as "todo" in Vitest output, none pass, none fail
    - Total new it.todo count across all 8 files: 54
  </acceptance_criteria>
  <done>5 component test stub files created with 29 total it.todo tests, npm test runs cleanly with all stubs in todo state</done>
</task>

</tasks>

<verification>
- `npm test` runs without errors and reports all new test suites
- All 8 test files exist in their correct `__tests__/` directories
- Total of 54 `it.todo` stubs across all files
- No test passes (all are in todo/pending state)
- Existing Phase 1 tests (session, validations, RegistrationForm, SessionGuard, register) still pass
</verification>

<success_criteria>
- 8 test stub files created matching VALIDATION.md Wave 0 requirements exactly
- npm test reports all suites, existing tests still green, new tests all todo
- Every TUTO requirement (01-06) has at least one test stub targeting it
</success_criteria>

<output>
After completion, create `.planning/phases/02-tutorial-shell-progress-engine/02-00-SUMMARY.md`
</output>
